    # python aik high level language hay ya Artificial intelligence ma bohat kaam aati hai
    # python aik indentation wali language hai
    #        indentation mean ka kis cheese ko kitna space deky lika hua hay (proper spacing)

    # variable wo hota hai jis ki value change ho jai
    # variable ka name ka under Uppercase, lowercase, digit or underscore aasakty hai yani hum ya sab use krky 
    #                                                 variable banasakty hain
    # variable ka name koi number sa start nahi ho sakta 
    #         Example =  variable1   // correct 
    #                    1variable   // incorrect
    # special symbol hum variable ma use nahi karsakty  (@,!,%,$,#)
    # Hum variable ka name bara bhi lik sakty hai python humy variable name rakny pa koi error nahi dega
    #     But aik achy programmer ki pehchan ya hote hai ka un ka variable ka name simple, meaning full hotay hai

    # python aik case sensitive language hai case sensitive mean ka (small a and capital A) alag alag hongay
    # python aik imlicite type language hai  imlicite mean jaha hum variable ka datatype nahi bataty

    # python ma agr situation kuch is tarha aajai jesy ya hai(not True and False or True) to yaad rahy ka pehle
    # not ko tarjee de jai ge pir and ko or last ma or ko 
    # Example:   // not True = false , 
    #           // false and false = false
    #          // false or true = true

    # single line if statment ko hum ternary operator bhi kahty hai

    # Conversion Humary pass 2 types ka hota hai
    #     Type Casting  and type Conversion
    #     Type Casting ma hum aik type ka variable ko dosry type ka variable zabrdasti yani manually convert karty hain
    #     type conversion ma hum aik type ka variable ko dosry type ka variable convert karty hain but conversion ma humy batana nahi partha hai
    # Type Conversion
    #     type Conversion jab hum aik type ka variable ko dosry type ka variable ma convert karty hain
    #     type conversion 2 types ka hota hai
    #         1. Implicit type conversion
    #         2. Explicit type conversion

    # python ma string ko number ma add nahi kia ja sakta

    # type Casting
    # Example:   
    # a = "4"
    # b = 6.23
    # c = int(a)
    # print(b + c)  // output = 10.23
    # is ko type casting kahty hain  ya humy manually karna parta hai

    #                                  Chapter 02
    # string ko hum 3 different methods sa store kar sakty hai(single comma, double comma, triple comma)
    # python ma spaces ko ignore Nahi kia jata us ko bhi count kia jata hai
    # string ma Humary pass positive and negitive dono indexing hote hai

    #     positive indexing
    #                     str = "My self hamza"
    #                     print(str[4:8])
    #                     print(str[:8])

    #     negitive indexing
    #     negitive slicing ma starting index print nahi hota

    #     string function
    #     string function ma sary functions ko print karty waqat round bracket lagany party hai
    #     endswith and startwith waly function ma agr hum koi string pass karty hain to wo humy true ya false return karty hain
    #     agr kisi bhi endswith ya startwith ki value ma hum zra barabr bhi changing nahi karsakty answer  sahi lany ka liay
    #     humy exact value pass karni parti hai (yani case sensitive) (startswith("h") or startswith("H"))
    #     (startswith("h") or startswith("H")) agr is tarha lika ho to answer false aai ga kyun ka is ma aik (h capital hai or aik small)
        
    #     // str = "I am studing python from youtube"
    #     // print(str.replace("am", "in"))  // replace function ma jo pehla argument hoga wo replace hoga dosra argument sa

    #     .find function ma humary pass word ka index aei ga  (agr word nahi mila to -1 aei ga)
    #     split function ma humy string ko split karna hota hai
    #     (her aik word ko alag alag string ma convert karna)
    #     Example:
    #     ['i', 'am', 'learning', 'python', 'from', 'youtube']

    #     str = "i am learning python from youtube"
    #     print(str.count("m")) # count function ma jo argument hoga wo kitni bar repeat ho raha ha wo count karaga

    #     conditional Statment  (if, elif, else)
    #     if statment humesha condition ka liay check kary ga
    #     elif statment jab bhi if statment false dedegi tab check kary ge
    #     else statment ma hum koi condition check Nahi karty matlab agr uper wali sary condition false ho jai to else statment chalay gi
        
    #     Nesting condition
    #         if ka under aik or if likhna
    #     Example 
    #         if(condition1):
    #             if(condition2):
    #                 print

    #             Chapter 03
    #         list = 
    #         list humary pass mutable hoti hai
    #         list ma humary pass square bracket hoty hain
    #         list ma hum kisi bhi type ki value store kar sakty hain
    #         list ma hum duplicate value bhi store kar sakty hain
    #         list ma humary pass positive and negitive dono indexing hoti hai
    #         list mutable hoti hai (yani hum list ma value change kar sakty hain)
    #         list ka under bhi slicing possible hay
    #         list ma .append method sa hum value add kar sakty hain last ma
    #         list ma .sort method sa hum value ko sort kar sakty hain (ascending order mean tarteeb sa likna)
    #         list = [40, 50, 10, 80, 100]
    #         list.sort(reverse=True)
    #         print(list)  # [100, 80, 50, 40, 10] descending order
    #         list ma .reverse method sa hum value ko reverse kar sakty hain
    #         list ma .pop method sa hum value ko remove kar sakty hain (last ma aik value remove ho jaye gi)
    #         list ma .remove method sa hum value ko remove kar sakty hain (jo value hum remove karna chah rahy hain us ka index pass karna parta hai)
    #         list ma .clear method sa hum sary value ko remove kar sakty hain(yani liat empty ho jaye ge)
    #         list ma .copy method sa hum list ko copy kar sakty hain
    #         list ma .insert method sa hum value add kar sakty hain kisi bhi index pa
    #         Example:
    #         list = [40, 50, 10, 80, 100]
    #         list.insert(2, 70)
    #         print(list)  #(is ma 70 3rd position pe add ho jaye ga 10 sa pehle)


    #                 tuples = ()
    #         tuples humary pass immutable hoti ha
    #         tuple ma humary pass round bracket hoty hay
    #         tuple ma agr humary pass aik value hay (Example: tuple = (1) to is tarha python is ko interger samjhe ga is liay humy 1 ka bad aik comma laga pary ga (tuple = (1,) is tarha))
    #         tuple ka under bhi slicing karn possible hay and same list ki tarha work karti hay

    #         palindrome wo hoti hai jo ulta bhi seedha hi hoti hai (Example: madam, level, radar)

    #         dictionary = {}
    #         dictionary ma humary pass curly bracket hoty hain
    #         dictionary ma humary pass key and value hoty hain (key: value)
    #         dictionary ma humary pass key unique hoti hain yani duplicate nahi hoti
    #         dictionary ma humary pass key ko access karne ka liay humy key ka naam likhna parta hai (Example: dict = {"name": "hamza"} print(dict["name"]))
    #         dictionary ma humary pass key ko update karne ka liay humy key ka naam likhna parta hai (Example: dict = {"name": "hamza"} dict["name"] = "ali" print(dict)  # output = {"name": "ali"})
    #         dictionary ma humary pass key ko delete karne ka liay humy del function use karna parta hai (Example: dict = {"name": "hamza"} del dict["name"] print(dict)  # output = {})
    #         dictionary ma humary pass key ko check karne ka liay humy in function use karna parta hai (Example: dict = {"name": "hamza"} print("name" in dict)  # output = True)
    #         dictionary ma humary pass key ko get karne ka liay humy get function use karna parta hai (Example: dict = {"name": "hamza"} print(dict.get("name"))  # output = hamza)
    #         dictionary ma multiple key and value store kar saktay hain (Example: dict = {"name": "hamza", "age": 20, "city": "karachi"}) and list array bhi store karsakty hai (Example: dict = {"name": "hamza", "age": 20, "city": ["karachi", "lahore"]})
    #         dictionary ma humary pass keys hote hay us ma hum list and dictionary ko use Nahi kar sakty (Example: dict = {"name": "hamza", "age": 20, "city": {"karachi": "lahore"}})
    #         duplicate mutable hote hain
    #         dictionary unordered hote hain yani is ma koi tarteeb nahi hota
    #         dictionary ma hum duplicate keys create Nahi kar sakty (Example: dict = {"name": "hamza", "name": "ali"} to is ma last wali value aei gi yani ali)

    #             sets = {}
    #         sets ma humary pass curly bracket hoty hain
    #         sets ma humary pass unordered hoty hain yani is ma koi tarteeb nahi hota
    #         sets humary pass mutable hoti hain yani is ma hum value change kar sakty hai
    #         sets ka element immutale hoty hain
    #         sets humary pass unique hoti hain yani duplicate nahi hoti
    #         sets ka under hum list and dictionary ko use nahi kar sakty (Example: sets = {1, 2, 3, [4, 5]} to is ma error aei ga kyun ka list ko set ma use nahi kia ja sakta)
    #         sets ma humary pass union hota hai yani hum multiple sets ko combine kar sakty hain (Example: set1 = {1, 2, 3} set2 = {4, 5, 6} set3 = set1.union(set2) print(set3) # output = {1, 2, 3, 4, 5, 6})
    #         sets ma empty set ka syntex (Example: set = set()) hota hai kyun ka sets ma curly bracket use hota hai to agar hum empty set ka syntex (Example: set = {}) likhen ge to wo dictionary samjhe ga is liay humy empty set ka syntex (Example: set = set()) likhna pary ga
    #         sets ma pop() method humary sets ka koi bhi random value delete kar dega

    #         Loops
    #         loop humary pass aik kaam bar bar karta hay yani lagatar karna
    #         humary pass loops ki types hote hay
    #         first hum while loop perhainy
    #         while loop ma humary pass break and continue bhi hota hay break ka matlab hota hay ka loop ko band kar do and continue ka matlab hota hay ka loop ko chalay do yani next iteration per chalay jao
    #         while loop ma humary pass condition hoti hay jab tak wo condition true hoti hay tab tak loop chalay ga
    #         while loop ma humary pass infinite loop bhi hota hay yani jab tak condition true hoti hay tab tak loop chalay ga (Example: while True: print("Hello Hamza"))
    #         Humy agr kisi iterater pa kaam karna hay mean ka changing karna yani update stoping condition to wo hum while loop se karenagy
    #         
    #         pass statment (Null statment)


    # function ko hum reduncy kam karny ka liay use karty hay
    # function ko hum jab proper likty hay us ko function defination kehty hay
    # parameter humary pass functions ma round brackets hoty hay and ma function call karty hay
    # arguments function call karty huay dety hay
    # python ka undr functions 2 type ka hoty hay (built in function, user defined function)

# recursion
#recursion loops ka danger version hota hay
# recursion loops sa easy code deta hay
# time 6:25:30






# Chapter 08
    # OOP
# procedural programming yani sequence sa sara code run hona pir function aaya us ne humy sikhaya ka reuseability redandency ko reduce karny ka liay function sa aik uper level ka method OOP hay
# is ma kisi bhi chees ko object banaya jasakta hay but object banany sa pehle us ki class banaty hay OOP ka under class blueprint hota hay
# Humary pass class ma jo bhi startup naming hoo usko hum capital letter sa start karengay
# class ko hum directly use nahi karsakty class ki pir humy object banani perhti hay(object ko intance kehty hay programming ke under) instance ya intances of class

    # Constructor
# constructor aik special function hota hay har class ka under jis ko hum init function kehty hay ya constructor bhi kehtay hay 
# ya object creation ka time pa invoke hota hay mean ka exacute hota hay
# python humary liay automatic init function create karti hay chahy ap init liko ya na liko but is ko hum likhty bhi hay
# constructor hamesha aik argument leta hay(yani parameter leta hay)first parameter hamesha self hota hay (Agr hum self ka name change karna chahty hain to kar saktay hain magar convention yehi hay ka self hi likha jai)
# constructor ke under hum multiple parameter bhi le saktay hay
# constructor har new object ka saat call hota hay
# jitna bhi data store kia jata hay usko hum attribute kehty hay
# constructor apny ap invoke ho jata hay yani call ho jata hay jab bhi hum new object create kartay hain (Hum agr object create karny ke bad usko print nahi karwaty or agr hum constructor ma print function laga detay hain to wo apny ap print ho jata hay)
# constructor ko hum 2 category ma divide kar saktay hain

#       1. Default constructor
# Default constructor ma aik hi parameter hota hay yani (Self wala) hota hay agr is hum nahi banai gay to isy python apny ap bana deta hay

#       2. Parameterized constructor
# is ke under self ka ilawa bhi parameter hotay hain jinko hum object create karny ke time pa value detay hain


#   instance attribute 
# instance attribute wo hota hay jo har object ka apna hota hay

#       Methods
# methods wo functions hotay hain jo class ke andar define kiye jate hain
# methods ko call karne ke liye hume object ka istemal karna parta hai
# methods ko call karte waqt pehle self parameter pass hota hai

        # Attribute
# attribute mean koi bhi data
# humary pass 2 type ka attribute hoty hay (class attribute, object attribute)
# class attribute objects ke liay common hota hay
# aik instance attribute har object ka apna hota hay
# instance attribute her aik object ka alg alg hota hay (example self.name, self.marks)
# agr humary pass aik class attribute hay or aik object attribute hay dono ka name same hay to object attribute ki value ko preference di jai gi yani wo print hoga
# Aesa data jo har object ka alg alg hota hay usko hum self. sa declare kartay hain
# Aesa data jo har object ka common hota hay usko hum class name sa declare kartay hain

        # Static Method
# static method wo hota hay jo class ke andar define kiya jata hay magar uska self parameter nahi hota
# is ma hum aik decorator lagaty hain @staticmethod

        # Abstraction 
# abstraction mean chupa hua data
# unnecessary data ko chhupana and essential data ko show karna abstraction kehlata hai

        # Encapsulation
# jab bhi hum koi class bana rahy hay us ka under attribute bhi hay and methods bhi hay to us ko hum encapsulation kehty hain

        # del keyword
# is keyword sa hum kisi bhi object ya us ki properties ko delete kartay hay delete 

        # private attriubute and method
# jis information ko hum class sa bahir pass karna nahi chahtay to us ko private karty hay

        # Inheritance
# parent to child ko pass karta hay
# child to parent ko nahi pass karta
# types of inheritance
#     1. single inheritance
#     2. multi level inheritance
#     3. hierarchical inheritance
#     4. multiple inheritance

# multiple inheritance ma aik child class hoti hay or uska 2 ya 2 sa zyada parent hotay hain

# Aik sa ziyada inheritance jab bhi use kartay hay to unko hybrid inheritance kehtay hay

        # super method
# ya aik special method hota hay classes ke undr

        # class method
# jo kaam staticmethod nahi kar pati us ko classmethod karti hay
# is ma hum aik decorator use kartay hain @classmethod

        # property decorator
# is decorator ka use hum getter and setter method banany k liay karty hain
# getter method ka use hum attribute ki value ko get karne k liay karty hain

        # polymorphism overloading
# polymrophism ka matlab hay aik sa ziyada form
# overloading ka matlab hay aik hi name ka function ya operator different different kaam karn
# time = 46:00